{
  "name": "@types/mz",
  "version": "1.0.0",
  "description": "Typings for mz",
  "scripts": {
    "build": "typings bundle -o test/bundle.d.ts",
    "test": "tsc -p test",
    "lint": "tslint -c tslint.json index.d.ts"
  },
  "author": "Felix Becker <felix.b@outlook.com>",
  "license": "ISC",
  "devDependencies": {
    "tslint": "^4.0.2",
    "typescript": "^2.0.9",
    "typings": "^2.0.0"
  }
}
